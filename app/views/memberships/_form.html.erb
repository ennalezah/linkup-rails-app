<%= simple_form_for membership, url: memberships_path, method: :post do |f| %>
  <%= f.input :interests, as: :text,
              autofocus: true,
              label: 'What topics would you like to discuss at events? (optional)' %>

  <%= f.hidden_field :user_id %>

  <%= f.hidden_field :group_id %>

  <%= f.submit 'Join group' %>
<% end %>