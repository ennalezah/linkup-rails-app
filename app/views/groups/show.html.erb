<div class="content">
  <h1><%= @group.name %></h1>
  <p><%= @group.description %></p>

  <p><%= link_to 'Edit', edit_group_path(@group) %> | <%= link_to 'Delete', group_path(@group), method: :delete, data: { confirm: "Are you sure you want to delete this group?" } %></p>



</div>